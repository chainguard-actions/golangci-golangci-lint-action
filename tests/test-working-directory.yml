name: Test working-directory input
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Install Go
        run: |
          if ! command -v go &>/dev/null; then
            apt-get update -qq && apt-get install -y golang-go
          fi
          go version
      - id: lint
        uses: .
        with:
          working-directory: actions/hardened/golangci--golangci-lint-action/v8.0.0/tests/fixtures/gosubproject
          args: "--config actions/hardened/golangci--golangci-lint-action/v8.0.0/tests/fixtures/gosubproject/.golangci.yml ./..."
          skip-cache: 'true'
      - name: Assert lint ran in working directory
        run: |
          echo "Lint step with working-directory completed without error"
          echo "PASS"
